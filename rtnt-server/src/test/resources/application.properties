rtnt.flow.live-enabled=false
# Dedicated database so tests never touch the local `rtnt` data. Host matches the
# app default (Docker on 27018). Override with TEST_MONGODB_URI if needed.
spring.mongodb.uri=${TEST_MONGODB_URI:mongodb://localhost:27018/rtnt-test}
