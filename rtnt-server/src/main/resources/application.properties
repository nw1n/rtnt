spring.application.name=rtnt
server.port=18080
# Initial world size (IslandSeeder ApplicationRunner @Order(1); defaults match code if omitted)
rtnt.startup.island-count=16
rtnt.map.width=2000
rtnt.map.height=1000
# Default live tick interval for new game_flow_status docs (ms per tick). Runtime value is stored in Mongo.
rtnt.flow.live-interval-ms=1000
rtnt.snapshot.interval-ticks=100
rtnt.island.economy-interval-ticks=100
rtnt.island.food-interval-ticks=500
rtnt.island.production-chance=0.25
rtnt.island.production-min=1
rtnt.island.production-max=5
rtnt.island.growth-goods-threshold=20
rtnt.island.population-growth-min=1
rtnt.island.population-growth-max=3
rtnt.startup.ship-count=60
rtnt.ship.journey-check-interval-ticks=5
# Persist each trade to Mongo (trade_events). Set false to skip writes on live ticks.
rtnt.trade.persist-events=false
# MongoDB Configuration
# Uses IntelliJ env var MONGODB_URI when set.
# Falls back to local Docker MongoDB when MONGODB_URI is missing.
spring.mongodb.uri=${MONGODB_URI:mongodb://localhost:27018/rtnt}
spring.data.mongodb.auto-index-creation=true
# Logging defaults tuned for lower log ingestion costs in Azure.
logging.level.root=${LOG_LEVEL_ROOT:WARN}
logging.level.com.example.rtnt=${LOG_LEVEL_APP:WARN}
logging.level.org.mongodb.driver=${LOG_LEVEL_MONGODB_DRIVER:WARN}
logging.level.org.springframework.data.mongodb.core.MongoTemplate=WARN
